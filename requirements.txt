# Hooze Outbound OS — Python processing layer dependencies
# Install with: pip install -r requirements.txt --break-system-packages
supabase==2.9.1        # Supabase client (postgrest + storage), used by every module's db.py
httpx==0.27.2           # HTTP client used by python/research/fetcher.py for public page fetches
beautifulsoup4==4.12.3  # HTML parsing for python/research/fetcher.py
pydantic==2.9.2         # Structured validation for AI input/output contracts (docs/07 §2, docs/08 §2, docs/12 §2)
python-dotenv==1.0.1    # Loads .env for local/dev runs (n8n injects real env vars in production)
pytest==8.3.3           # Test runner for tests/
fastapi==0.115.0        # HTTP layer for python/api.py — only needed if deploying Python as its own
                        # service (e.g. Railway, see RAILWAY_DEPLOYMENT.md). Not needed for the
                        # single-VPS Execute Command deployment in DEPLOYMENT.md.
uvicorn==0.31.0          # ASGI server that runs python/api.py
